import * as React from 'react';
import Seo from '../components/seo';

const Index = ({ location }) => {
    return (
        <div>
            <Seo title="Index" />
        </div>
    );
};

export default Index;
